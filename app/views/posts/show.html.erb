<p id="notice"><%= notice %></p>

      <div class="container">
        <div class="row">
             <div class="col s12">
               <div class="card">
                 <p>
                   <%= @post.user.name %>
                 </p>

                 <p>
                   <%= @post.city.name %>
                 </p>

                 <!-- card title -->
                 <div class="card-image">
                     <%= image_tag "tbnail.jpg"%>
                     <span class="card-title"><%= @post.title %></span>
                 </div>

                 <!-- card content -->
                 <div class="card-content">
                     <%= @post.user.name %>
                     <%= @post.description %>
                 </div>

                 <!-- card action -->
                 <div class="card-action">
                   <a href="#">Te hago la gauchada</a>
                 </div>
               </div>
             </div>
           </div>
      </div>
