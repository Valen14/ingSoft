<%= form_for @achievement, :html => {:class => 'col s12'} do |f| %>

		<div class="row">
			<div class="input-field col s12">
				<%= f.label "nombre", class:"active"  %>
  				<%= f.text_field :name , class: 'validate'%>
  			</div>
  		</div>

		<div class="row">
			<div class="input-field col s12">
				<%= f.label "puntos minimos", class:"active" %>
  				<%= f.text_field :point_min , class: 'validate', label: 'puntos minimos' %>
  			</div>
  		</div>

		<div class="row">
			<div class="input-field col s12">
				<%= f.label "puntos maximos", class:"active" %>
  				<%= f.text_field :point_max , class: 'validate', label: 'puntos maximos' %>
  			</div>
  		</div>
  		<%= f.submit "Guardar", class: 'btn waves-effect waves-light' %>
  		<%= link_to "Volver", achievements_path, class: 'btn waves-effect waves-light' %>
  	</div>
<% end %>
